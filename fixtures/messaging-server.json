{
  "name": "messaging-server",
  "description": "Team communication helper.",
  "tools": [
    {
      "name": "send_slack_message",
      "description": "Post a message to a Slack channel.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "channel": {
            "type": "string"
          },
          "message": {
            "type": "string"
          }
        },
        "required": ["channel", "message"]
      }
    },
    {
      "name": "read_mentions",
      "description": "Read inbox notifications and thread mentions.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "channel": {
            "type": "string"
          }
        }
      }
    }
  ]
}
